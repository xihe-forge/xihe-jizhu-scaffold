# Bug Workflow

1. Record the bug and reproduction steps.
2. Identify the root cause before patching.
3. Fix the smallest layer that resolves the issue.
4. Verify the fix.
5. Capture the learning in `dev/knowledge-base/` if reusable.
