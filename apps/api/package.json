{
  "name": "@xihe-loom-scaffold/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@xihe-loom-scaffold/shared": "workspace:*",
    "@xihe-loom-scaffold/types": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch --preserveWatchOutput",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "prettier --check \"src/**/*.{ts,tsx}\"",
    "test": "echo \"No app-level tests — run pnpm test:unit from root\""
  }
}
